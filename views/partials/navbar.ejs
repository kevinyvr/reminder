  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">Remindly</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <% if ((typeof(showNavBar) != 'undefined') && (showNavBar == "reminder")) { %>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/auth/myprofile">My Profile</a>
          <a class="nav-item nav-link" href="/reminders">Reminder List</a>
          <a class="nav-item nav-link" href="/reminder/new">New Reminder</a>
          <a class="nav-item nav-link" href="/auth/logout">Log Out</a>
        </div>
      </div>
      <% } %>
      <% if ((typeof(showNavBar) != 'undefined') && (showNavBar == "admin")) { %>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/auth/myprofile">My Profile</a>
          <a class="nav-item nav-link" href="/auth/logout">Log Out</a>
        </div>
      </div>
      <% } %>
    </div>
  </nav>